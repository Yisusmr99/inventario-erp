{
  "name": "inventario-erp",
  "version": "1.0.0",
  "description": "Proyecto ERP de inventario",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.2.0"
  }
}
